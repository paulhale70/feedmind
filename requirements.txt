# FeedMind - All dependencies are optional
# The core app (feedmind.py) runs with zero pip installs.

# --- PDF export ---
reportlab>=4.0.0

# --- Desktop notifications ---
plyer>=2.1.0

# --- Podcast audio playback ---
# Not needed on Windows (built-in MCI backend is used automatically).
# Install only if pygame is preferred or you're on macOS/Linux.
# pygame-ce>=2.5.0

# --- Audio metadata (optional, for duration info) ---
# Not needed on Windows (MCI queries duration directly).
mutagen>=1.47.0

# --- AI-powered summarization ---
anthropic>=0.18.0    # Claude API
openai>=1.12.0       # OpenAI API

# --- Full-text article extraction ---
newspaper3k>=0.2.8
trafilatura>=1.6.0
