# FeedMind - All dependencies are optional
# The core app (feedmind.py) runs with zero pip installs.

# --- Security hardening (strongly recommended) ---
# defusedxml: blocks XXE and billion-laughs attacks when parsing OPML and
#             untrusted remote RSS/Atom feeds. Without it, FeedMind falls
#             back to xml.etree and logs a warning at startup.
defusedxml>=0.7.1
# keyring:    stores Claude/OpenAI API keys in the OS credential manager
#             (Windows Credential Manager, macOS Keychain, freedesktop
#             Secret Service) instead of plaintext SQLite. Without it,
#             keys fall back to the settings table and emit a warning.
keyring>=24.0.0

# --- PDF export ---
reportlab>=4.0.0

# --- Desktop notifications ---
plyer>=2.1.0

# --- Podcast audio playback ---
# Not needed on Windows (built-in MCI backend is used automatically).
# Install only if pygame is preferred or you're on macOS/Linux.
# pygame-ce>=2.5.0

# --- Audio metadata (optional, for duration info) ---
# Not needed on Windows (MCI queries duration directly).
mutagen>=1.47.0

# --- AI-powered summarization ---
anthropic>=0.18.0    # Claude API
openai>=1.12.0       # OpenAI API

# --- Full-text article extraction ---
newspaper3k>=0.2.8
trafilatura>=1.6.0
